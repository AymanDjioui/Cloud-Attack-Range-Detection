# ── Private files (reports, prompts, passwords) ──
_private/
.env

# ── Terraform state (contains sensitive data) ──
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup

# ── LocalStack runtime data ──
localstack-data/

# ── Node modules ──
node_modules/

# ── Python cache & bytecode ──
__pycache__/
*.pyc

# ── OS files ──
.DS_Store
Thumbs.db

# ── IDE ──
.vscode/
.idea/
.gemini/
